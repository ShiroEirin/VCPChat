# Configuration for VCPEverything Plugin (HTTP Mode)

# --- Everything HTTP Server ---
# Port number for Everything's built-in HTTP server.
# Make sure Everything is running and HTTP server is enabled:
# Tools -> Options -> HTTP Server -> Enable HTTP server
EVERYTHING_PORT=8025

# --- Behavior ---
# Enable debug mode for more verbose logging to stderr. (true/false)
DEBUG_MODE=true
